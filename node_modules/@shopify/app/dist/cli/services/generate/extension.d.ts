import { AppInterface } from '../../models/app/app.js';
import { GenericSpecification } from '../../models/app/extensions.js';
import { UIExtensionSpec } from '../../models/extensions/ui.js';
import { DependencyVersion } from '@shopify/cli-kit/node/node-package-manager';
interface ExtensionInitOptions<TSpec extends GenericSpecification = GenericSpecification> {
    name: string;
    app: AppInterface;
    cloneUrl?: string;
    extensionFlavor?: ExtensionFlavor;
    specification: TSpec;
    extensionType: string;
}
interface ExtensionDirectory {
    extensionDirectory: string;
}
export declare type ExtensionFlavor = 'vanilla-js' | 'react' | 'typescript' | 'typescript-react' | string;
declare type UIExtensionInitOptions = ExtensionInitOptions<UIExtensionSpec> & ExtensionDirectory;
declare function extensionInit(options: ExtensionInitOptions): Promise<string>;
export declare function getRuntimeDependencies({ specification, extensionFlavor, }: Pick<UIExtensionInitOptions, 'specification' | 'extensionFlavor'>): DependencyVersion[];
export default extensionInit;
